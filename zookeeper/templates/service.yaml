apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service
  labels:
    service: {{ .Values.name }}
spec:
  type: ClusterIP
  ports:
  - port: 2181
    targetPort: 2181
    name: connect

  selector:
    deploy: {{ .Values.name }} 