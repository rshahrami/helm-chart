apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service
  labels:
    app: {{ .Values.name }}
spec:
  ports:
  - port: 80
    name: web
  ports:
  - port: 80
    name: web
  clusterIP: None
  selector:
    app: {{ .Values.name }}
